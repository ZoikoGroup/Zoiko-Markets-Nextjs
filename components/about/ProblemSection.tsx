import { AboutSection } from "./shared";

export function ProblemSection() {
  return (
    <AboutSection
      id="problem"
      eyebrow="The problem"
      title="Good businesses often outlive the ownership structures built for them."
      description="Short holding periods and generic playbooks push specialist businesses toward decisions that trade away their long-term strength."
    />
  );
}
