import { AboutSection } from "./shared";

export function InfrastructureSection() {
  return (
    <AboutSection
      id="infrastructure"
      eyebrow="Infrastructure"
      title="Shared infrastructure, independent operations."
      description="Portfolio companies keep their own teams and decisions while drawing on shared data, technology and operating resources."
    />
  );
}
