import { AboutSection } from "./shared";

export function TrustAssetSection() {
  return (
    <AboutSection
      id="trust-as-an-asset"
      eyebrow="Trust as an asset"
      title="Trust compounds the same way capital does."
      description="Every portfolio company operates under the same commitment to reliability, transparency and long-term thinking."
      tone="dark"
    />
  );
}
