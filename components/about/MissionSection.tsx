import { AboutSection } from "./shared";

export function MissionSection() {
  return (
    <AboutSection
      id="mission"
      eyebrow="Our mission"
      title="Grow specialist businesses that compound value over decades, not quarters."
      description="Zoiko Markets exists to give focused, well-run companies permanent, patient ownership."
    />
  );
}
